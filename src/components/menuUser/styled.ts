import styled from "styled-components";

const MenuUserWrapper = styled.div`
  display: flex;
  align-items: center;
  min-width: 100px;
  justify-content: space-around;
`;

export default MenuUserWrapper;
